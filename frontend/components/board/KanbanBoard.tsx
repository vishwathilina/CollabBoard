export function KanbanBoard({ workspaceId }: { workspaceId: string }) {
  return (
    <div className="rounded-xl border border-dashed border-border bg-surface p-6 text-sm text-muted">
      KanbanBoard stub — Member 5 (workspace {workspaceId})
    </div>
  );
}
