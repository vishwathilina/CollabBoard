export function WorkTree({ workspaceId }: { workspaceId: string }) {
  return (
    <div className="rounded-xl border border-dashed border-border bg-surface p-6 text-sm text-muted">
      WorkTree stub — Member 3 (workspace {workspaceId})
    </div>
  );
}
